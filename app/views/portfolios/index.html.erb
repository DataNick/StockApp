<h1>Listing Portfolios</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Amount</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody id = "portfolios">
    <%= render @portfolios %>
  </tbody>
</table>

<br>

<%= react_component 'Portfolios', { data: @portfolios } %>



<%= link_to 'New Portfolio', new_portfolio_path %>
